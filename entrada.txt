//pre: cierto
//post: max es el elemento máximo des e i es la posición en la que

//      se encuentra.
VARIABLES
    k: NUM;
    j: LOG;
    v: SEQ(NUM);

SUBPROGRAMAS

    FUNCION mayor(SEQ(NUM) s, NUM q) dev (NUM i,NUM max)
        VARIABLES
            m: NUM;
        INSTRUCCIONES
            q = 10;
            m = 1;
            s = [51,2];
            q = s[0];
            mostrar(3+4, q);
            mientras(m>0) hacer
                k=q;
                m = -1;
            fmientras
            dev 0;
    FFUNCION

    PROCESO a (SEQ(NUM) s, NUM q)
        INSTRUCCIONES
            a = mayor(s,q)

INSTRUCCIONES
    k[1]=q;