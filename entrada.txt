//pre: cierto
//post: max es el elemento máximo des e i es la posición en la que

//      se encuentra.
VARIABLES
    k,i,b: NUM;
    j: LOG;

SUBPROGRAMAS
    FUNCION mayor(SEQ(NUM) s,LOG abc) dev (NUM res1,NUM max)
        VARIABLES
            sa2: SEQ(NUM);
            q: LOG;
            qqq: LOG;
        INSTRUCCIONES
            mientras(!s[k]) hacer
                i=j;
            fmientras
    FFUNCION

    FUNCION func2() dev (LOG func2_res)
        VARIABLES
            qa1: NUM;
            q: NUM;
        INSTRUCCIONES
            qa1 = 50;
            q = 10;
    FFUNCION

    PROCEDIMIENTO prod1(NUM arg1)
        VARIABLES
            k: NUM;
        INSTRUCCIONES
            k=T;
    FPROCEDIMIENTO

INSTRUCCIONES
    k=F;