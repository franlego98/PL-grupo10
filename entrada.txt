//pre: cierto
//post: max es el elemento máximo des e i es la posición en la que

//      se encuentra.
VARIABLES
    k,i,b: NUM;
    j: LOG;

SUBPROGRAMAS
    FUNCION mayor(SEQ(LOG) s,LOG abc) dev (NUM res1,NUM max)
        VARIABLES
            sa2: SEQ(NUM);
            q: LOG;
            qqq: LOG;
        INSTRUCCIONES
            mientras(!s[k]) hacer
                i=k*b;
                ruptura;
            fmientras
            s = [T,F,T,F];
            dev 1,k;
    FFUNCION

    FUNCION func2() dev (NUM res2,LOG a)
        VARIABLES
            qa1: NUM;
            q: LOG;
        INSTRUCCIONES
            qa1 = 50;
            dev 10,T;
    FFUNCION

    PROCEDIMIENTO prod1(NUM arg1)
        VARIABLES
            ka: LOG;
        INSTRUCCIONES
            ka=T;
    FPROCEDIMIENTO

INSTRUCCIONES
    k,j = func2();
    k=-25;