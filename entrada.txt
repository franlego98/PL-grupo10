//pre: cierto
//post: max es el elemento máximo des e i es la posición en la que

//      se encuentra.
VARIABLES
    k: NUM;
    j: LOG;

SUBPROGRAMAS
    FUNCION mayor(SEQ(NUM) s) dev (NUM i,NUM max)
        VARIABLES
            j: NUM;
        INSTRUCCIONES
            q = 10;
            mientras(s[j]>max) hacer
                i=j;
            fmientras
    FFUNCION

INSTRUCCIONES
    k[1]=q;